import { Card } from "antd";
import styled from "styled-components";

export const StyledSiteGeneralConfigCard = styled(Card)`
  & .btn-more {
    position: absolute;
    right: 20px;
    top: 20px;
    padding: 0px;
  }
`